# This file defines all module routes (Higher priority routes first)
#
# import these routes in the main app as :
# *     /                       module:clientside
#
# ~~~~

GET		/routes					play.modules.clientside.ClientSideController.exportAllRoutes
GET		/routes/{category}
GET   /routes/all.js    play.modules.clientside.ClientSideController.allJsRoutes